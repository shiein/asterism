{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window and protected recording toolbar can call desktop commands",
  "windows": ["main", "capture-toolbar"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
